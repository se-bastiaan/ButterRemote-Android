box: jacekmarchwicki/android
# Build definition
build:
    steps:
        - script:
            name: run gradle
            code: gradle --full-stacktrace -q --project-cache-dir=$WERCKER_CACHE_DIR assembleDebug lintDebug